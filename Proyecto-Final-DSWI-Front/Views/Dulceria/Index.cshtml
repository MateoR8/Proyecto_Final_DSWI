@model IEnumerable<Proyecto_Final_DSWI_Front.Models.Dulceria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nombre_producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.precio_producto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.marca_producto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_producto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_producto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.precio_producto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.marca_producto)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.id_producto}) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.id_producto })
            </td>
        </tr>
}
    </tbody>
</table>
